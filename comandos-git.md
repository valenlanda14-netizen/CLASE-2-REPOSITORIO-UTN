git init agregar archivos
git add versionar o comitear
git commit -m "comentario" guardar cambios
git status ver el estatus actual de nuestro repositorio
git log ver historial de commits
git branch -M main renombrar rama maestra a main
git rm --cached <archivo> eliminar un archivo (transformarlo en untrackeable)